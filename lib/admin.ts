// Single admin identity. The client uses this only to show/hide the admin UI;
// the /api/admin/* routes enforce it server-side against a verified ID token.
export const ADMIN_EMAIL = 'rrasel141@gmail.com'
